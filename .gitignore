__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
venv/
.venv/
options.json
*.log

# Runtime SQLite database. Rebuilt from data/*.json on first start
# (db.py init_db) and holds real usage: users, query_log, inspections.
su_urunleri_kolluk.db

# Ana kaynak klasörü (mevzuat, rehberler, Excel ve kurum içi yayınlar) yalnızca
# bu bilgisayarda tutulur; depo herkese açık olduğu için commit edilmez.
# Uygulamanın çalışması için gereken kamuya açık Markdown kopyası
# su_urunleri_bot/data/markdown/ altında paketlenir.
SU ÜRÜNLERİ KAYNAKLAR (MARKDOWN)/

# Bu bilgisayara özel canlı sistem notları ve yardımcı betikler (adresler,
# KeenDNS adı). Depo herkese açık olduğu için asla commit edilmez.
yerel/

# Claude Code yerel izin listesi (bu bilgisayara özel).
.claude/settings.local.json
