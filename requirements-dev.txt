# Development and test tooling.
# Not installed into the addon image; use locally with:
#   pip install -r requirements-dev.txt
# The add-on itself has no runtime dependencies (standard library only).

pytest>=7.4.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1

black>=23.0.0
isort>=5.12.0
flake8>=6.0.0
mypy>=1.5.0

python-dotenv>=1.0.0
pyyaml>=6.0
